{
  "name": "romola-cloud",
  "version": "2.0.0",
  "private": true,
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "cd backend && npm install",
    "start": "cd backend && node server.js"
  }
}